/*
 * Arago reveal.js plugin — STUB (Phase 0).
 *
 * Le plugin réel (Phase 4, cf. arago-spec §4.6) lira `?aragoRoom=PIN&aragoSecret=...`
 * dans l'URL de la deck, se connectera au WebSocket Arago, émettra les events
 * `slideChanged` / `fragmentShown` et recevra les commandes `next`/`prev`/`goto`/`togglePause`.
 *
 * Inclus tel quel, ce stub ne fait rien d'autre que se signaler dans la console.
 */
(function () {
  'use strict';
  console.info('[arago-reveal-plugin] stub chargé — pilotage distant disponible en Phase 4.');
})();
